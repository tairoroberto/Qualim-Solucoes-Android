<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.tairoroberto.qualimsolucoes" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InicialActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/title_activity_principal" >
        </activity>
        <activity
            android:name=".CronogramaActivity"
            android:label="@string/title_activity_cronograma" >
        </activity>
        <activity
            android:name=".RelatoriosActivity"
            android:label="@string/title_activity_relatorios" >
        </activity>
        <activity
            android:name=".PrestacaoContasActivity"
            android:label="@string/title_activity_prestacao_contas" >
        </activity>
    </application>

</manifest>
